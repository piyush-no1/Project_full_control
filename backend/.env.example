# Required for custom gesture script generation
GROQ_API_KEY=

# Required to enforce Firebase ID token audience verification in backend
FIREBASE_PROJECT_ID=
# Optional: service account file path for backend Firestore writes
FIREBASE_SERVICE_ACCOUNT_PATH=

# Optional: override Mediapipe hand-landmarker task path
HAND_LANDMARKER_MODEL_PATH=
